import { array } from "../input";

export const addition = (a,b) => a+b;
export const func1 = toto => toto;

export const func2 = () => true;
export const transform = (arr) => arr.map((a,index) => `Nombre ${index} : ${a}`);
